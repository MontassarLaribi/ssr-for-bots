{
  "name": "ssr-for-bots",
  "version": "1.0.0",
  "description": "SSR-for-bots is an express middleware that will apply SSR to your application only when bots are visiting it",
  "main": "./lib/index.js",
  "scripts": {
    "compile": "tsc",
    "lint": "tslint -c tslint.json 'src/**/*.{ts,tsx}'"
  },
  "keywords": [
    "ssr",
    "express",
    "middleware",
    "puppeteer"
  ],
  "author": "Montassar Laribi",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^5.5.0",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/puppeteer": "^5.4.2"
  }
}
